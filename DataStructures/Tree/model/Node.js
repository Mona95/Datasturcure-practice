//Node represent each node of the binary search tree
export class Node {
  constructor(key) {
    this.key = key; //node value
    this.left = null; // left child node reference
    this.right = null; // right child node reference
  }
}
